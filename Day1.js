

    //second Task
alert("Welcome! This is the first release of our calculator.");

//  first number
var firstNumber = prompt("Enter the first number:");

//  second number
var secondNumber = prompt("Enter the second number:");

// Convert the  strings to numbers and calculate the sum
var sum = parseFloat(firstNumber) + parseFloat(secondNumber);

// Display the summation result
alert(`${firstNumber} + ${secondNumber} = ${sum}`);
